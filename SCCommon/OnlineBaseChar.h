#ifndef	_ONLINE_BASECHAR_H
#define _ONLINE_BASECHAR_H

/*
class	OnlineBaseChar
{
protected:
	SI32		m_siX, m_siY;				// 캐릭터의 좌표.
	UI16		uiKind;						// 캐릭터의 종류.
	UI16		uiUniqueID;					// 계정 ID.(1 ~ 65535)
public:
	OnlineBaseChar();
	virtual	~OnlineBaseChar();

	virtual	VOID	SetPos(SI32 siX, SI32 siY);				// 새로운 좌표로 설정한다.
	virtual	VOID	GetPos(SI16 *psiX, SI16 *psiY);			// 좌표를 얻어온다.
	virtual	UI16	GetUniqueID();							// UniqueID를 얻어온다.
};
*/

#endif
